{
    "version": 6,
    "configurePresets": [
        {
            "name": "linux",
            "hidden": true,
            "condition": {
                "type": "equals",
                "lhs": "${hostSystemName}",
                "rhs": "Linux"
            }
        }
    ],
    "buildPresets": [
        {
            "name": "linux",
            "hidden": true,
            "configurePreset": "linux"
        }
    ]
}
